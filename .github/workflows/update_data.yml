name: Rassim DZ Auto-Bot (Titanium Max Edition)

on:
  schedule:
    - cron: '0 * * * *' # تشغيل آلي كل ساعة بدقة متناهية
  workflow_dispatch: # للتشغيل اليدوي الفوري عند الحاجة

jobs:
  scrape_and_sync:
    runs-on: ubuntu-latest
    
    # تحديد الصلاحيات للسماح بالدفع (Push) التلقائي
    permissions:
      contents: write

    steps:
      - name: 📥 سحب الكود (Checkout Code)
        uses: actions/checkout@v4 # تحديث للإصدار الأحدث لضمان السرعة

      - name: 🐍 إعداد بيئة بايثون (Python 3.11)
        uses: actions/setup-python@v4
        with:
          python-version: '3.11' # التوافق مع بيئة الـ Dev Container
          cache: 'pip' # تفعيل التخزين المؤقت لتسريع العملية

      - name: 📦 تثبيت المكتبات (Dependencies)
        run: |
          python -m pip install --upgrade pip
          pip install pandas requests beautifulsoup4 sqlalchemy lxml

      - name: 🔍 تشغيل محرك الجمع (Run Scraper)
        # أضفت لك متغيرات بيئة هنا في حال احتجت لاستخدام مفاتيح API مستقبلاً
        run: python myscraper.py
        env:
          PYTHONPATH: .
          DB_PATH: "rassim_titanium_max_2026.db"

      - name: 💾 حفظ وتحديث البيانات (Sync Database)
        run: |
          git config --global user.name "Rassim Titanium Bot"
          git config --global user.email "bot@rassimdz.com"
          
          # إضافة كل من ملف CSV وقاعدة البيانات لضمان المزامنة الكاملة
          git add rassim_titanium_max_2026.db users_database.csv
          
          # التحقق مما إذا كان هناك تغييرات فعلاً لتجنب الأخطاء
          if git diff --staged --quiet; then
            echo "لا توجد عروض جديدة في الـ 58 ولاية حالياً."
          else
            git commit -m "🚀 تحديث ذكاء السوق | 📅 $(date +'%Y-%m-%d %H:%M') | 🇩🇿 58 ولاية"
            git push origin HEAD:${{ github.ref }}
          fi

      - name: 🚨 تقرير الفشل (Failure Report)
        if: failure()
        run: echo "حدث خطأ في عملية الجمع التلقائي، يرجى مراجعة سجلات myscraper.py"
